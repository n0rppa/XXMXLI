name: Image optimize

on:
  push:
    paths:
      - 'assets/images/**'

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: |
          npm install sharp
      - name: Run optimizer (placeholder)
        run: |
          node -e "console.log('Image optimization step placeholder')"
